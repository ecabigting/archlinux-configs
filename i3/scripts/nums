#!/usr/bin/env bash

status="$(xset -q | grep Num | awk '{ print $8 }')"
lockstatus=""
color="#cad3f5"
bgcolor="#24273a"

if [ "$status" == "off" ]; then
    lockstatus=" 0  "
    color="#cad3f5"
    bgcolor="#24273a"
else
    lockstatus=" 0  "
    color="#ffffff"
    bgcolor="#181926"
fi

echo ${lockstatus}
echo ${lockstatus}
echo ${color}
echo ${bgcolor}